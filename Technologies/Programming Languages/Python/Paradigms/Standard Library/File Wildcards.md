The `glob` [[Python Standard Library|module]] provides a function for making file lists from directory wildcard searches:
```Python
import glob
glob.glob('*.py')  # ['primes.py', 'random.py', 'quote.py']
```
