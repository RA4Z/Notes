To loop over a sequence in reverse in [[🐍Python]], first specify the sequence in a forward direction and the call the `reversed()` function.

```Python
for i in reversed(range(1, 10, 2)):
    print(i)

# Output: 9
# Output: 7
# Output: 5
# Output: 3
# Output: 1
```