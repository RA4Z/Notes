The `pop()` method is used to remove and return and element of a [[Lists|List]]

```Python
# Initialize a list
fruits = ['apple', 'banana', 'cherry']
# Remove the last element
last_fruit = frutas.pop()
print(last_fruit) # Output: cherry
print(fruits) # Output: ['apple', 'banana']
```