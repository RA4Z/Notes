To append a value to a [[Lists|List]] in Python at the end of the [[Lists|List]], it's possible to use `append()`

```Python
# Initialize a list
fruits = ["apple", "banana", "cherry"]
# Append a new value
fruits.append("orange")
print(fruits) # Output: ['apple', 'banana', 'cherry', 'orange']
```