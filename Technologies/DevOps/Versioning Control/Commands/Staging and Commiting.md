[[Git]] commands to prepare changes for committing

- `git add`: Stages changes from the working directory to the staging area
- `git commit`: Records staged changes as a new commit in the repository's history
- `git rm`: Removes files from the working directory and stages the removal
- `git mv`: Renames or moves files and stages the changes