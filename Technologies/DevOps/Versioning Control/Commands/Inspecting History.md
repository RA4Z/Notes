[[Git]] commands to view the repository's history and compare different versions of files or commits

- `git log`: Show the commit history
- `git diff`: Show changes between various states
- `git show`: Shows information about a specific commit or object