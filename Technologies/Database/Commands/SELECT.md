It is an [[SQL]] statement used to retrieve data from a relational database, allowing the developer to query and retrieve specific information from one or mode tables.

```SQL
SELECT *
FROM Customers;
```

