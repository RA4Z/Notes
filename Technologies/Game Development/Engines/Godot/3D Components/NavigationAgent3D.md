In [[Godot]], **NavigationAgent3D** is used to pathfind to a position while avoiding obstacles. The calculation can be used by the parent node to dynamically move it along the path. Requires navigation data to work correctly.


